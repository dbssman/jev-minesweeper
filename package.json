{
	"name": "jev-minesweeper",
	"version": "0.1.0",
	"private": true,
	"description": "Minesweeper played by TypeSafe's Jev System One model: one P(mine) per hidden cell per move, code owns the policy.",
	"type": "module",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"start": "node server.mjs",
		"dev": "node server.mjs",
		"test": "node --test",
		"eval": "node run.mjs",
		"ablate": "node ablate.mjs"
	}
}
